import IconDrawStrategy from "./IconDrawStrategy"
import ListDrawStrategy from "./ListDrawStrategy"

const DrawStrategy = {
    Icon: IconDrawStrategy,
    List: ListDrawStrategy
}

export default DrawStrategy